{% extends 'base.html.twig' %}

{% block title %}ListaPrecio{% endblock %}

{% block body %}
    <h1>ListaPrecio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lista_precio.id }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ lista_precio.precio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lista_precio_index') }}">back to list</a>

    <a href="{{ path('app_lista_precio_edit', {'id': lista_precio.id}) }}">edit</a>

    {{ include('lista_precio/_delete_form.html.twig') }}
{% endblock %}
