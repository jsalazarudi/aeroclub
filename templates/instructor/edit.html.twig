{% extends 'theme.html.twig' %}

{% block title %}Editar Instructor{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-5">Editar Instructor</h1>

        {{ include('instructor/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <a class="btn btn-secondary mb-3" href="{{ path('aeroclub_instructor_index') }}">Regresar</a>
        {{ include('instructor/_delete_form.html.twig') }}
    </div>

{% endblock %}
