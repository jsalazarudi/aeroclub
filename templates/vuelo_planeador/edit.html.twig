{% extends 'theme.html.twig' %}

{% block title %}Editar Vuelo Planeador{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Vuelo Planeador</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_vuelo_planeador_index') }} ">Regresar</a>
            {{ include('vuelo_planeador/_delete_form.html.twig') }}
        </div>

        {{ include('vuelo_planeador/_form.html.twig') }}
    </div>
{% endblock %}
