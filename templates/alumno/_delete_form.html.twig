<form method="post" action="{{ path('aeroclub_alumno_delete', {'id': alumno.id}) }}" onsubmit="return confirm('Esta seguro de desactivar al alumno?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alumno.id) }}">
    <button class="btn btn btn-danger">Desactivar</button>
</form>