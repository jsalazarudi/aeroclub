{{ form_start(form,{'attr': {'class': 'p-2' }}) }}
    <div class="row">
        <div class="col mb-2">
            <label class="text-muted mb-2 fs-3" for="">{{ form_label(form.tipo_usuario) }}</label>
        </div>
    </div>
    <div class="row">
        <div class="col mb-3">
            {{ form_widget(form.tipo_usuario) }}
        </div>
    </div>

    <button class="btn btn-primary">{{ button_label|default('Crear Usuario') }}</button>
{{ form_end(form) }}
