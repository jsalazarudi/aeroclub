{% extends 'base.html.twig' %}

{% block title %}VueloMotor{% endblock %}

{% block body %}
    <h1>VueloMotor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vuelo_motor.id }}</td>
            </tr>
            <tr>
                <th>Aterrizajes</th>
                <td>{{ vuelo_motor.aterrizajes }}</td>
            </tr>
            <tr>
                <th>Pista_despegue</th>
                <td>{{ vuelo_motor.pistaDespegue }}</td>
            </tr>
            <tr>
                <th>Pista_aterrizaje</th>
                <td>{{ vuelo_motor.pistaAterrizaje }}</td>
            </tr>
            <tr>
                <th>Horometro_despegue</th>
                <td>{{ vuelo_motor.horometroDespegue }}</td>
            </tr>
            <tr>
                <th>Horometro_aterrizaje</th>
                <td>{{ vuelo_motor.horometroAterrizaje }}</td>
            </tr>
            <tr>
                <th>Tipo_vuelo</th>
                <td>{{ vuelo_motor.tipoVuelo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vuelo_motor_index') }}">back to list</a>

    <a href="{{ path('app_vuelo_motor_edit', {'id': vuelo_motor.id}) }}">edit</a>

    {{ include('vuelo_motor/_delete_form.html.twig') }}
{% endblock %}
