{% extends 'base.html.twig' %}

{% block title %}Venta{% endblock %}

{% block body %}
    <h1>Venta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ventum.id }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ ventum.observaciones }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ ventum.fecha ? ventum.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_venta_index') }}">back to list</a>

    <a href="{{ path('app_venta_edit', {'id': ventum.id}) }}">edit</a>

    {{ include('venta/_delete_form.html.twig') }}
{% endblock %}
