{% extends 'theme.html.twig' %}

{% block title %}Editar Venta{% endblock %}

{% block content %}
    <div class="container">

        {% if is_granted('ROLE_SOCIO') or is_granted('ROLE_PILOTO') %}
            <h1 class="text-center mb-2">Editar Compra</h1>
        {% else %}
            <h1 class="text-center mb-2">Editar Venta</h1>
        {% endif %}

        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_venta_index') }} ">Regresar</a>
        </div>

        {{ include('venta/_form.html.twig') }}
    </div>
{% endblock %}
