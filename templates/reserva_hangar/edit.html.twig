{% extends 'theme.html.twig' %}

{% block title %}Editar Reserva Hangar{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Reserva Hangar</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_reserva_hangar_index') }} ">Regresar</a>
{#            {{ include('reserva_hangar/_delete_form.html.twig') }}#}
        </div>

        {{ include('reserva_hangar/_form.html.twig') }}
    </div>
{% endblock %}
