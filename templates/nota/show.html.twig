{% extends 'base.html.twig' %}

{% block title %}Nota{% endblock %}

{% block body %}
    <h1>Nota</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notum.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ notum.descripcion }}</td>
            </tr>
            <tr>
                <th>Fecha_recordatorio</th>
                <td>{{ notum.fechaRecordatorio ? notum.fechaRecordatorio|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nota_index') }}">back to list</a>

    <a href="{{ path('app_nota_edit', {'id': notum.id}) }}">edit</a>

    {{ include('nota/_delete_form.html.twig') }}
{% endblock %}
