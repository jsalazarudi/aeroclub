{% extends 'theme.html.twig' %}

{% block title %}Editar Nota{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Nota</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_nota_index') }} ">Regresar</a>
            {{ include('nota/_delete_form.html.twig') }}
        </div>

        {{ include('nota/_form.html.twig') }}
    </div>
{% endblock %}
