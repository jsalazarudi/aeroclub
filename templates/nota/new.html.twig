{% extends 'theme.html.twig' %}

{% block title %}Crear Nota{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-2">Crear Nota</h1>
        <div class="text-center">
            <a class="btn btn-secondary" href="{{ path('app_nota_index') }}">Regresar</a>
        </div>
        {{ include('nota/_form.html.twig') }}
    </div>

{% endblock %}



