{{ form_start(form,{'attr': {'class': 'p-2' }}) }}
<div class="row">
    <div class="col mb-3">
        {{ form_label(form.socio) }}

        {% if form_errors(form.socio) %}
            <div class="alert alert-danger">
                {{ form_errors(form.socio) }}
            </div>
        {% endif %}

        {{ form_widget(form.socio) }}
    </div>
    <div class="col mb-3">
        {{ form_label(form.servicio) }}

        {% if form_errors(form.servicio) %}
            <div class="alert alert-danger">
                {{ form_errors(form.servicio) }}
            </div>
        {% endif %}

        {{ form_widget(form.servicio) }}
    </div>
</div>
<div class="row">
    <div class="col mb-3">
        {{ form_label(form.hasta) }}

        {% if form_errors(form.hasta) %}
            <div class="alert alert-danger">
                {{ form_errors(form.hasta) }}
            </div>
        {% endif %}

        {{ form_widget(form.hasta) }}
    </div>
</div>
<div class="text-center mt-3">
    <button class="btn btn-primary btn-lg">{{ button_label|default('Guardar') }}</button>
</div>
{{ form_end(form) }}
