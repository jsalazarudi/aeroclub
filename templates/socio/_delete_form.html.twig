<form method="post" action="{{ path('aeroclub_socio_delete', {'id': socio.id}) }}" onsubmit="return confirm('Esta seguro de desactivar al socio?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ socio.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
