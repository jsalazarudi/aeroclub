{% extends 'theme.html.twig' %}

{% block title %}Editar Socio{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-5">Editar Socio</h1>

        {{ include('socio/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <a class="btn btn-secondary mb-3" href="{{ path('aeroclub_socio_index') }}">Regresar</a>
        {{ include('socio/_delete_form.html.twig') }}
    </div>

{% endblock %}
