{% extends 'theme.html.twig' %}

{% block title %}Editar Tesorero{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-5">Editar Tesorero</h1>

        {{ include('tesorero/_form.html.twig', {'button_label': 'Actualizar'}) }}

        <a class="btn btn-secondary mb-3" href="{{ path('aeroclub_tesorero_index') }}">Regresar</a>
        {{ include('tesorero/_delete_form.html.twig') }}
    </div>

{% endblock %}
