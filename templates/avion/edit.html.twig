{% extends 'theme.html.twig' %}

{% block title %}Editar Avión{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Avión</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_avion_index') }} ">Regresar</a>
            {% if is_granted('ROLE_ADMINISTRADOR') %}
                {{ include('avion/_delete_form.html.twig') }}
            {% endif %}
        </div>

        {{ include('avion/_form.html.twig') }}
    </div>
{% endblock %}
