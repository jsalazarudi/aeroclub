{% extends 'theme.html.twig' %}

{% block title %}Crear Avión{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-2">Crear Avión</h1>
        <div class="text-center">
            <a class="btn btn-secondary" href="{{ path('app_avion_index') }}">Regresar</a>
        </div>
        {{ include('avion/_form.html.twig') }}
    </div>

{% endblock %}