{% extends 'theme.html.twig' %}

{% block title %}Editar Historial Lista Precio{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Historial Lista Precio</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_historial_lista_precio_index') }} ">Regresar</a>
            {% if is_granted('ROLE_ADMINISTRADOR') %}
                {{ include('historial_lista_precio/_delete_form.html.twig') }}
            {% endif %}
        </div>

        {{ include('historial_lista_precio/_form.html.twig') }}
    </div>
{% endblock %}
