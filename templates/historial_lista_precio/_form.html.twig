{{ form_start(form,{'attr': {'class': 'p-2' }}) }}
<div class="row">
    <div class="col mb-3">
        {{ form_label(form.porcentaje_cambio) }}

        {% if form_errors(form.porcentaje_cambio) %}
            <div class="alert alert-danger">
                {{ form_errors(form.porcentaje_cambio) }}
            </div>
        {% endif %}

        {{ form_widget(form.porcentaje_cambio) }}
    </div>
    <div class="col mb-3">
        {{ form_label(form.fecha) }}

        {% if form_errors(form.fecha) %}
            <div class="alert alert-danger">
                {{ form_errors(form.fecha) }}
            </div>
        {% endif %}

        {{ form_widget(form.fecha) }}
    </div>
</div>
<div class="text-center mt-3">
    <button class="btn btn-primary btn-lg">{{ button_label|default('Guardar') }}</button>
</div>
{{ form_end(form) }}
