{% extends 'theme.html.twig' %}

{% block title %}Crear Servicio{% endblock %}

{% block content %}

    <div class="container">
        <h1 class="text-center mb-2">Crear Servicio</h1>
        <div class="text-center">
            <a class="btn btn-secondary" href="{{ path('app_servicio_index') }}">Regresar</a>
        </div>
        {{ include('servicio/_form.html.twig') }}
    </div>

{% endblock %}



