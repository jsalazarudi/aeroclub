{% extends 'theme.html.twig' %}

{% block title %}Editar Servicio{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Servicio</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_servicio_index') }} ">Regresar</a>
            {{ include('servicio/_delete_form.html.twig') }}
        </div>

        {{ include('servicio/_form.html.twig') }}
    </div>
{% endblock %}
