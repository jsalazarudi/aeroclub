{% extends 'base.html.twig' %}

{% block title %}Abono{% endblock %}

{% block body %}
    <h1>Abono</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abono.id }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ abono.valor }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ abono.fecha ? abono.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Aprobado</th>
                <td>{{ abono.aprobado ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_abono_index') }}">back to list</a>

    <a href="{{ path('app_abono_edit', {'id': abono.id}) }}">edit</a>

    {{ include('abono/_delete_form.html.twig') }}
{% endblock %}
