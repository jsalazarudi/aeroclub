{% extends 'base.html.twig' %}

{% block title %}MovimientoStock{% endblock %}

{% block body %}
    <h1>MovimientoStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movimiento_stock.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ movimiento_stock.cantidad }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ movimiento_stock.tipo }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ movimiento_stock.observaciones }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movimiento_stock_index') }}">back to list</a>

    <a href="{{ path('app_movimiento_stock_edit', {'id': movimiento_stock.id}) }}">edit</a>

    {{ include('movimiento_stock/_delete_form.html.twig') }}
{% endblock %}
