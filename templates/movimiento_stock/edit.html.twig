{% extends 'theme.html.twig' %}

{% block title %}Editar Movimiento Stock{% endblock %}

{% block content %}
    <div class="container">
        <h1 class="text-center mb-2">Editar Movimiento Stock</h1>
        <div class="text-center">
            <a class="btn btn-secondary mb-3" href="{{ path('app_movimiento_stock_index') }} ">Regresar</a>
            {{ include('movimiento_stock/_delete_form.html.twig') }}
        </div>

        {{ include('movimiento_stock/_form.html.twig') }}
    </div>
{% endblock %}
